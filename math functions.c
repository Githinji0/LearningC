#include <stdio.h>
#include <math.h>  //note include to use special math functions

int main()
{ 
	double A = sqrt(144); //square root
	double B = pow(3, 6); // powerr
	int C = round(3.14); //round off
	
	int D = ceil(3.14);
	int E = floor(3.17);
	
	double F = fabs(-100);
	double G = log(34);
	
	double H = sin(90);
	double I = tan(45);
	double J + cos(30);
	
	printf("\n%.2lf", B);// change accordingly
	
	return 0;
}