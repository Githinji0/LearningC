#include <stdio.h>

int main()
{
	// they are used to replace a statement where an operator takes a variable as one of its arguments and then assigns the result back to the same varisabel x = x = 1 
	// x+=1;
	
	int x = 10;
	//x+=2;

x/=5;
 printf("%d", x);
	return 0;
}